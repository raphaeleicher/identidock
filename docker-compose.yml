identidock:
  build: .
  ports:
    - "5000:5000"
  environment:
    ENV: DEV
    #no_proxy: localhost,dnmonster
    #NO_PROXY: localhost,dnmonster
  volumes:
    - ./app:/app
  links:
    - dnmonster
    - redis

dnmonster:
  image: amouat/dnmonster:1.0

redis:
  image: redis:3.0

